# context.yaml — auto-generated by state.py — do NOT edit manually
snapshot: "2026-07-01T12:08:33"

phase:
  current: none
  all:
    [x] survey
    [x] map
    [x] audit
    [x] draft
    [-] stabilize
    [-] derive
    [-] extend

# --- SESSION CONTINUITY (critical for cold-start) ---
next_action: "lancer /audit — scoring OWASP + qualite + maintenabilite, ecrire logs/audit/latest.md"
active_file:  "src/app.py"
turn_count: 0
compact_due: false  # 0 turns remaining

decisions_recent:

smells:
  open: 22
  fixed: 0

tests:
  total: 0
  passing: 0
  coverage_pct: 0

progress:
  modules_migrated: 0
  modules_total: 5

risks:
  - "Suppression en cascade (delete_project supprime tasks) sans transaction atomique"
  - "export_data() ecrit un fichier JSON non protege sur le serveur, accessible par chemin connu"
  - "Hardcoded secret_key - les sessions Flask sont forgeables par qui connait le secret"

knowledge_stack:
  [x] src/app.py                          # ~3224 tokens — raw legacy code
  [x] logs/audit/survey.md                # ~2754 tokens — derived: smell inventory
  [x] logs/audit/map.md                   # ~4592 tokens — derived: dependency map
  [x] logs/audit/latest.md                # ~3075 tokens — derived: technical scoring
  [x] logs/audit/draft.md                 # ~4846 tokens — derived: refactor plan

# context_profiles: which files to inject per phase
context_profiles:
  survey:    [CLAUDE.md, context.yaml, system.md, src/app.py]
  map:       [CLAUDE.md, context.yaml, system.md, survey.md]
  audit:     [CLAUDE.md, context.yaml, system.md, survey.md, map.md]
  draft:     [CLAUDE.md, context.yaml, system.md, conventions.md, data_contract.md, latest.md]
  stabilize: [CLAUDE.md, context.yaml, system.md, draft.md, src/app.py]
  derive:    [CLAUDE.md, context.yaml, system.md, conventions.md, coding_standards.md, data_contract.md, latest.md, draft.md]
  compact:   [CLAUDE.md, context.yaml, history.md]
  doctor:    [CLAUDE.md, context.yaml, history.md]
